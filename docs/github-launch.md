# GitHub Launch Pack

## Suggested Repository Description

Copy-ready rules and a tiny CLI for Codex, Claude Code, Cursor, Windsurf, and GitHub Copilot.

## Before You Click Publish

- Replace `your-name` in `README.md` GitHub Actions badge links.
- Set the final repository name if it changes from `awesome-ai-coding-agent-rules`.
- Add a social preview image in GitHub repository settings.
- Add the suggested topics below.

## Suggested Topics

`ai`
`agents`
`coding-agent`
`codex`
`claude-code`
`cursor`
`windsurf`
`copilot`
`prompt-engineering`
`developer-tools`

## Suggested Pinned Demo

```bash
npx agent-rules-kit init cursor --preset web-app
npx agent-rules-kit doctor
```

## Launch Post

```text
I built Agent Rules Kit.

It is a zero-dependency CLI plus a set of copy-ready rules for Codex, Claude Code, Cursor, Windsurf, and GitHub Copilot.

It helps AI coding agents:
- inspect the codebase before editing
- keep changes scoped
- follow local patterns
- run focused verification
- produce better PR reviews

Example:
npx agent-rules-kit init cursor --preset web-app

Would love feedback on which rule packs or presets should come next.
```
